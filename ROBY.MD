# Полное резюме: реверс-инжиниринг «Новый Робинзон» (Nikita, 1999)

## Структура файлов игры

```
Новый Робинзон/
├── Roby.exe          — главный исполняемый (525312 байт, PE32, 29.11.1999)
├── NGI32.dll         — движок (337920 байт, PE32, 12.04.1999)
├── NAV32.dll         — аудио/видео (33792 байт)
├── MiniGame.dll      — мини-игры
└── DATA/
    ├── BAR/BAR.DAT   — инвентарная панель (85 ресурсов)
    ├── SCEN/         — сцены (.DAT файлы)
    ├── CHAR/         — персонажи (.DAT файлы)
    ├── WAVE/         — звук
    ├── MOVIE/        — видео (AVI)
    ├── startup.dan   — стартовый скрипт
    └── 0.ngb         — NGB файл
```

---

## Формат .DAT (контейнер ресурсов)

### Заголовок (0x20 байт, открытый текст)
| Смещение | Размер | Содержимое |
|---|---|---|
| 0x00 | 2 | Magic `NL` |
| 0x02 | 2 | Версия (0x0001) |
| 0x04 | 2 | Число ресурсов |
| 0x14 | 4 | Ключ LFSR для расшифровки таблицы ресурсов |

### Таблица ресурсов (с 0x20, зашифрована LFSR, 32 байта на запись)
| Смещение в записи | Размер | Содержимое |
|---|---|---|
| 0–11 | 12 | Имя ресурса (null-terminated ASCII) |
| 12–15 | 4 | Неизвестно |
| 16–17 | 2 | FLAGS (0x0080 = Huffman+LZSS) |
| 18–19 | 2 | KEY (передаётся в ngiUnpack) |
| 20–23 | 4 | Размер декомпрессированных данных |
| 24–27 | 4 | Абсолютное смещение данных в файле |
| 28–31 | 4 | Размер сжатых данных |

### Алгоритм расшифровки таблицы (LFSR)
```python
def lfsr_decrypt(data, key_dword):
    al = key_dword & 0xff
    dl = (key_dword >> 8) & 0xff
    out = bytearray()
    for b in data:
        al = (al << 1) & 0xff
        al ^= dl
        dl >>= 1
        out.append(b ^ al)
        dl ^= al
    return bytes(out)
```

---

## Формат .NGB (Nikita Graphic Bitmap)

Декомпрессированный NGB начинается с заголовка:

| Смещение | Тип | Содержимое |
|---|---|---|
| 0–1 | int16 | x_left (координата на экране) |
| 2–3 | int16 | y_top |
| 4–5 | int16 | x_right |
| 6–7 | int16 | y_bottom |
| 8 | byte | flags |
| 9+ | bytes | пиксели 8bpp (indexed color) |

Размеры: `width = x_right - x_left + 1`, `height = y_bottom - y_top + 1`.

Палитра хранится в отдельном ресурсе `.COL` (1024 байт = 256 цветов × 4 байта BGRA в Windows RGBQUAD формате).

---

## Алгоритм сжатия NGI (Huffman + LZSS)

### Параметры
- **Кольцевой буфер**: 4096 байт, инициализирован пробелами (0x20)
- **Начальная позиция ring**: 0xFC4 (для FLAGS & 0x80)
- **Символов в дереве (N_CHAR)**: 314
  - Символы 0–255: литеральные байты
  - Символы 256–313: back-references (длина матча = symbol - 253, то есть 3..60)
- **Узлов в дереве (TN)**: 627 = 2×314 - 1
- **ROOT**: 626
- **MAX_FREQ**: 0x8000 (порог перестройки дерева)

### Критически важно: глобальное состояние
Кольцевой буфер и дерево Хаффмана **глобальные** и **не сбрасываются** между ресурсами и между файлами. Все ресурсы всех `.DAT` файлов декомпрессируются как один непрерывный поток. Это означает что нельзя декомпрессировать BAR.DAT ресурс 42 не декомпрессировав сначала ресурсы 0–41 в правильном порядке, и не зная состояния ring buffer от предыдущих `.DAT` файлов.

### Bit buffer
Используется **единый 32-битный bit buffer** (`ebp` в x86 коде) и счётчик `cl`. Для символов используются биты через `shl bp,1` (16-bit), для позиций через `shl ebp,8` (32-bit). Это один и тот же регистр, одна очередь бит из одного потока данных.

### Таблица позиций
Строится из двух таблиц констант в NGI32.DLL:
```
t1 = [1, 3, 8, 12, 24, 16, 32, 48, 64, 48, 48, 16]  # RVA 0x043210
t2 = [32, 48, 64, 48, 48, 16]                          # RVA 0x043216
```

`pos_table[256]` — базовые значения позиций (DWORD, кратные 0x400000).
`pos_len_table[256]` — количество дополнительных бит (1–6).

Финальная позиция: `pos = ((ebp & 0x3f0000) | pos_table[prefix]) >> 16`

### Инициализация дерева (точно как в NGI32)
```python
N_CHAR = 314; TN = 627; ROOT = 626; MAX_FREQ = 0x8000

freq = [0] * (TN + 1)
prnt = [0] * (TN + N_CHAR + 1)
son  = [0] * TN

for i in range(N_CHAR):
    freq[i] = 1
    son[i] = i + TN
    prnt[i + TN] = i

i2, j2 = 0, N_CHAR
while j2 <= ROOT:
    freq[j2] = freq[i2] + freq[i2+1]
    son[j2] = i2
    prnt[i2] = prnt[i2+1] = j2
    i2 += 2; j2 += 1

freq[TN] = MAX_FREQ + 1
prnt[ROOT] = 0
```

---

## Экспорты NGI32.DLL (ключевые функции)

### Ресурсы
| Ординал | Имя | Описание |
|---|---|---|
| 80 | rsOpenLib(filename, flags) | Открыть .DAT файл, возвращает lib handle |
| 81 | rsCloseLib | Закрыть |
| 82 | rsLoad(lib, index) | Загрузить ресурс по индексу, возвращает ptr на данные |
| 84 | rsGetInfo | Информация о ресурсе |
| 103 | rsFind | Найти по имени |

### Рендеринг
| Ординал | Имя | Описание |
|---|---|---|
| 16 | vrtPutNgb | Отрисовать NGB спрайт |
| 17 | vrtPutDib | Отрисовать DIB |
| 18 | vrtStretchNgb | Растянутый NGB |
| 23 | vrtTextOut | Вывод текста |
| 12 | vrtBind / vrtUnbind | Bind render target |
| 13 | vrtFlush | Flush |

### Ядро
| Ординал | Имя | Описание |
|---|---|---|
| 51 | ngiUnpack(flags, key, dst, decomp_size, src, src_size) | Декомпрессия |
| 54 | ngiAlloc | Выделить память |
| 45 | ngiFree | Освободить |
| 39 | ngiSetPalette | Установить палитру |
| 35/36 | ngiGetNgbWidth/Height | Размеры NGB |

### Сигнатура ngiUnpack
```c
void ngiUnpack(
    int   flags,       // 0x80 = Huffman+LZSS
    int   key,         // entry[18..19]
    void* dst_buf,     // output buffer
    int   decomp_size, // размер выходных данных
    void* src_ptr,     // сжатые данные
    int   src_size     // размер сжатых данных
);
```

---

## Скриптовый язык .DAN

Файлы `.DAN` начинаются с магии `NL 00 01`. Команды (из ROBY.EXE):

`AddItem`, `AddVar`, `Aproach`, `ClearScreen`, `CreateObject`, `DelObject`, `DeleteItem`, `ENDIF`, `EndGame`, `GoScene`, `HideChar`, `Interrupt`, `LockBar`, `Set`, `SetActive`, `SetBar`, `SetCharVar`, `SetDir`, `SetMap`, `SetMouse`, `SetMusic`, `SetRest`, `SetVar`, `SetVert`, `Shift`, `ShiftScreen`, `ShowChar`, `ShowCursor`, `ShowNav`, `Sound`, `StartGame`, `Text`

Строки в CP1251, команды перемежаются байтами-опкодами (< 0x20).

---

## Формат BAR.DAT (инвентарная панель)

85 ресурсов:
| Индексы | Имена | Описание |
|---|---|---|
| 0 | BAR0.NGB | Вся панель 640×80 (51216 байт) |
| 1–3 | BAR1–3.NGB | ~4500 байт каждый |
| 4 | BAR4.NGB | 8074 байт |
| 5 | BAR5.NGB | 12736 байт |
| 6–71 | BAR6–71.NGB | Спрайты предметов 48×60 пикселей (2896 байт) |
| 72–76 | BAR72–76.NGB | 4570 байт |
| 77–82 | BAR77–82.NGB | 1446–1584 байт |
| 83 | FONT.NFT | Шрифт (21618 байт) |
| 84 | BAR.COL | Палитра 256 цветов BGRA (1024 байт) |

---

## Рабочий способ извлечения ресурсов (AutoIt + NGI32.DLL)

Самый надёжный способ — вызывать оригинальную DLL через AutoIt на Windows (тестировалось на Windows 2000 в 86Box):

```autoit
Local $gamedir = "C:\Program Files\Nikita\Новый Робинзон\"
Local $hDll = DllOpen($gamedir & "NGI32.dll")

; Открыть .DAT файл
Local $ret = DllCall($hDll, "ptr", "rsOpenLib", "str", $gamedir & "DATA\BAR\BAR.DAT", "int", 0)
Local $lib = $ret[0]

; Получить ресурс по индексу
Local $ret2 = DllCall($hDll, "ptr", "rsLoad", "ptr", $lib, "int", 0)
Local $dataptr = $ret2[0]

; Сохранить в файл (N байт)
Local $struct = DllStructCreate("byte[51216]", $dataptr)
FileWrite("C:\output.bin", DllStructGetData($struct, 1))
```

---

## Что ещё предстоит для порта

1. **Декомпрессор** — логика bit buffer (единый `ebp:cl`) восстановлена теоретически, но точная реализация требует пошагового сравнения с оригиналом. Пока используй AutoIt для извлечения.

2. **Скриптовый движок** — опкоды `.DAN` файлов частично восстановлены. Нужно декодировать полный байткод.

3. **Рендеринг** — NGB формат понятен, нужно реализовать blitter с поддержкой прозрачности (цвет 0 = прозрачный).

4. **Звук** — через NAV32.dll, одна функция `navCreateInstance`.

5. **Порядок загрузки** — игра загружает файлы в определённом порядке, это влияет на состояние глобального ring buffer. Нужно воспроизвести точный порядок.

6. **Глобальное состояние** — ring buffer и Huffman tree должны быть **одни на всё время жизни движка**, как в оригинале.

---

## Инструменты которые пригодятся

- **OllyDbg** — отладчик под Windows, использовался для анализа
- **AutoIt** — для вызова NGI32.DLL и извлечения данных
- **86Box** — эмулятор PC с Windows 2000 для запуска игры
- **Capstone** — Python дизассемблер для анализа NGI32.DLL
- **Pillow** — Python для работы с изображениями
